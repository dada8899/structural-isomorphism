OPENROUTER_API_KEY=sk-or-v1-xxx
LLM_MODEL=anthropic/claude-sonnet-4.6
STRUCTURAL_DATA_DIR=/Users/wanqh/projects/structural-isomorphism/data
STRUCTURAL_KB_FILE=kb-5000-merged.jsonl
STRUCTURAL_MODEL_PATH=/Users/wanqh/projects/structural-isomorphism/models/structural-v1
PORT=5004

***REMOVED*** M1.4 (session ***REMOVED***16) — HMAC secret for share tokens. MUST be stable
***REMOVED*** across deploys; rotating breaks every existing share URL. Generate
***REMOVED*** once with `python -c "import secrets; print(secrets.token_hex(32))"`.
***REMOVED*** Hard-required in prod (STRUCTURAL_ENV=prod) — the code raises if
***REMOVED*** unset since the dev fallback is predictable from cwd.
STRUCTURAL_SHARE_TOKEN_SECRET=replace-me-with-64-hex-chars
