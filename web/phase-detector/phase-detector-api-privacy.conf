[Service]
# Uvicorn's default access line contains the complete request target. Nginx
# already provides path-only request telemetry, so disable this duplicate log.
ExecStart=
ExecStart=/root/Projects/structural-isomorphism-v4/.venv/bin/uvicorn v4.product.d1_phase_detector.api.main:app --host 127.0.0.1 --port 8200 --no-access-log
