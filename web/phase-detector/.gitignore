# dependencies
/node_modules
/.pnp
.pnp.js
# Lockfiles intentionally NOT tracked — the project pins versions in
# package.json and the deploy pipeline regenerates the lockfile per env.
package-lock.json
yarn.lock
pnpm-lock.yaml

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts.bak

# vercel
.vercel

# storybook static build (output of `pnpm storybook:build`)
/storybook-static
